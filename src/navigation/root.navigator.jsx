import MainNavigator from "./main.navigator";

/**
 * Root Navigator
 * Entry point for the application
 * Routes users through the main navigation system
 */
const RootNavigator = () => {
  return <MainNavigator />;
};

export default RootNavigator;
